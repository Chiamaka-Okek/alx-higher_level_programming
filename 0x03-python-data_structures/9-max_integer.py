#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list is []:
        return None
    else:
        for x in range(len(my_list)):
            if mylist[x] > my_list[x++]:
                
                return x
            else:

